# Web server
PORT=3000
STATIC_DIR=./public          # built frontend output

# Forecast cache (Railway: mount a volume here)
CACHE_DIR=/data/cache         # local dev: ./.cache

# Open-Meteo (free non-commercial tier needs no key). For commercial use only:
# OPEN_METEO_BASE=https://customer-api.open-meteo.com
# OPEN_METEO_KEY=
